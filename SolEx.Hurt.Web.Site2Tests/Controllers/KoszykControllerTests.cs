
using Xunit;

namespace SolEx.Hurt.Web.Site2Tests.Controllers
{
    public class KoszykControllerTests
    {
        [Fact()]
        public void DodajPozycjeTest()
        {
            //int idkoszyka = 1;
            //WartoscLiczbowa liczba = (decimal)100878.011345345;
            ////WartoscLiczbowa zaokraglona = System.Math.Round(liczba, 2);

            //List<Dodawany> lista = new List<Dodawany>();
            //for (int i = 0; i < 5; i++)
            //{
            //    lista.Add(new Dodawany() { Produkt = i + 1, Jednostka = i + 1, Ilosc = 5, Indywidualizacja = null });
            //}

            //List<koszyki_pozycje> dane = new List<koszyki_pozycje>();
            //foreach (Dodawany d in lista)
            //{
            //    dane.Add(new koszyki_pozycje { produkt_id = d.Produkt, ilosc = d.Ilosc, jednostka = d.Jednostka, koszyk_id = idkoszyka, Indywidualizacja = d.Indywidualizacja });
            //}


            //List<OdpowiedzKoszyk> listaOdp = new List<OdpowiedzKoszyk>();
            //OdpowiedzKoszyk odKosz = A.Fake<OdpowiedzKoszyk>();
            //A.CallTo(() => odKosz.Netto).Returns(liczba);
            ////A.CallTo(() => odKosz.Waluta).Returns(null);
            //listaOdp.Add(odKosz);


            //List<IKoszykPozycja> przekroczoneStany;
            //List<IKoszykPozycja> przekroczoneLimity;
            //List<IKoszykPozycja> zmienioneilosci;
            //List<IKoszykPozycja> dodane;
            //List<ProduktBazowy> nowe;


            //Koszyki k = new Koszyki();
            //k.id = idkoszyka;

            //IKlient klient = A.Fake<IKlient>();
            //A.CallTo(() => klient.klient_id).Returns(1);

            //var koszykiDostep = A.Fake<IKoszykiDostep>();
            //A.CallTo(() => koszykiDostep.PobierzIdAktualnegoKoszyka(klient, null)).Returns(idkoszyka);
            
            //A.CallTo(
            //    () =>
            //        koszykiDostep.Update(dane, klient, out przekroczoneStany, out przekroczoneLimity, out nowe, out zmienioneilosci, out dodane))
            //    .Returns(null);
            //A.CallTo(
            //    () =>
            //        koszykiDostep.WygenerujKomunikaty(null, new List<IKoszykPozycja>(), new List<IKoszykPozycja>(), new List<ProduktBazowy>(), new List<IKoszykPozycja>(), 
            //            new List<IKoszykPozycja>())).Returns(listaOdp);


            //KoszykController kc = new KoszykController();
            //kc.KoszykiDostep = koszykiDostep;

            //JsonResult result = kc.DodajPozycje(lista);
            ////result
            













        }
    }
}
