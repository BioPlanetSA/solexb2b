@using SolEx.Hurt.Core.BLL
@using SolEx.Hurt.Web.Site2.Helper
@model IList<SolEx.Hurt.Model.SubkontoGrupa>
<table class="table table-condensed">
    <thead>
        <tr>
            <th>@Html.PobierzTlumaczenie("Nazwa")</th>
            <th></th>
        </tr>
    </thead>
    <tfoot style="display:table-header-group">
        <tr>
            <th>
                <input type="text" class="form-control" />
            </th>
        </tr>

    </tfoot>
    <tbody>
        @foreach (var y in Model)
        {
            <tr>
                <td>
                    @y.Nazwa
                </td>
              
                <td>
                    @Html.ActionLink(SolexBllCalosc.PobierzInstancje.Konfiguracja.PobierzTlumaczenie(SolexHelper.AktualnyJezyk.Id, "Edytuj"), "Edycja", new { id = y.Id }, new { @class = "okno-modalne btn btn-secondary btn-sm" })
                    @Html.ActionLink(SolexBllCalosc.PobierzInstancje.Konfiguracja.PobierzTlumaczenie(SolexHelper.AktualnyJezyk.Id, "Usuń"), "Usun", new { id = y.Id }, new { @class = "btn btn-danger btn-sm btn-usun" })
                </td>
            </tr>
        }
    </tbody>
</table>